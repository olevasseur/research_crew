# Memory Snapshot — 20260328-224449-967316

## Working Memory

# Working Memory

## Current goal
Harden the RAG pipeline; CrewAI is not the active focus.

## Latest progress
- Added/evolved RAG hardening pieces: MMR-based selection in `analysis.py`, `rag/evaluate.py` + `rag_cli.py evaluate`, and `rag/navigation.py` (`trace`/`explore`).
- `navigation.py` was tested/fixed end-to-end on `digital-minimalism`; output formatting is now readable with no known regressions.
- `fiction-whois` is no longer blocked: real `pale-criminal` / `Gunther` CLI flow now passes, reads `fiction_state.json`, respects chapter cutoff, and prints requested fields.
- Added regression coverage for `fiction-whois`; pytest passes and real-command checks pass.
- Light output-quality hardening done for `fiction-whois`; field labels validated and no obvious future-info leakage seen at chapter 10.

## Open questions
- Is the `digital-minimalism` summary actually good enough? Eval exists but still needs review.
- Is CrewAI being retained at all, or effectively dead code?
- For `fiction-whois`, do we need one more fixture/assertion for schema variation or output labels, or is current coverage sufficient?

## Active assumptions
- `llama3.1` via Ollama is sufficient for current summarization experiments.
- Summary quality has not been meaningfully reviewed yet beyond generating one eval.
- If prompts change, bump `PROMPT_VERSION` from `v4`.
- `fiction_state.json` may still be irregular enough that current tests miss some real-world schema variation.

## What matters next
- Review the `digital-minimalism` evaluation output and decide whether summary quality is acceptable.
- Add a second book to exercise multi-book `compare` and `ask` flows.
- If eval reveals weaknesses, revise summary prompts/selection and bump `PROMPT_VERSION`.
- Optional follow-up: add one more `fiction-whois` fixture/assertion for schema variation or formatting regression protection.## Iteration 0 · 2026-03-28 18:15:57 UTC

**Progress:** Review the existing digital-minimalism RAG evaluation artifacts and add a lightweight, repeatable way to inspect summary quality from the CLI without changing core summarization behavior yet.

**Decisions:** approved — executor exit 0. Validation: `source .venv/bin/activate && pytest -q tests` → missing_tool; `source .venv/bin/activate && python rag_cli.py --help` → missing_tool; `source .venv/bin/activate && python rag_cli.py review-eval digital-minimalism || python rag_cli.py show-eval digital-minimalism || true` → missing_tool.

**Assumptions:** Evaluation artifact formats may be inconsistent or partially ad hoc, so the main risk is overfitting the parser to one file shape. Keep parsing defensive and prefer structured files when present. Also avoid introducing a command name that conflicts with existing CLI structure.

**Open questions:**
- Missing tool/dep for `source .venv/bin/activate && pytest -q tests` — needs env fix
- Missing tool/dep for `source .venv/bin/activate && python rag_cli.py --help` — needs env fix
- Missing tool/dep for `source .venv/bin/activate && python rag_cli.py review-eval digital-minimalism || python rag_cli.py show-eval digital-minimalism || true` — needs env fix

**Next:** If this lands cleanly, the next iteration should use the new review flow to inspect `digital-minimalism` quality and then decide whether prompt/selection changes are warranted; only after that should we consider bumping `PROMPT_VERSION` or adding a second book for compare/ask flows.

---

## Iteration 0 · 2026-03-28 22:03:27 UTC

**Progress:** Finish and verify the small CLI quality-review improvement by stabilizing the new evaluation review command/tests and addressing the previously observed missing-tool validation issue with environment-aware commands.

**Decisions:** approved — executor exit 0. Validation: `.venv/bin/pytest -q tests/test_review_eval.py` → missing_tool; `.venv/bin/pytest -q tests` → missing_tool; `.venv/bin/python rag_cli.py --help` → missing_tool; `.venv/bin/python rag_cli.py review-eval digital-minimalism` → missing_tool.

**Assumptions:** Main risk is overfitting the review parser to one existing evaluation artifact shape; keep parsing defensive and output best-effort summaries. Secondary risk is CLI naming/argument wiring conflicting with existing commands. Environment risk remains if `.venv` is absent or incomplete, but validation commands now avoid shell-activation assumptions.

**Open questions:**
- Missing tool/dep for `.venv/bin/pytest -q tests/test_review_eval.py` — needs env fix
- Missing tool/dep for `.venv/bin/pytest -q tests` — needs env fix
- Missing tool/dep for `.venv/bin/python rag_cli.py --help` — needs env fix
- Missing tool/dep for `.venv/bin/python rag_cli.py review-eval digital-minimalism` — needs env fix

**Next:** If this lands and validates, the next iteration should use the new review command on `digital-minimalism` to inspect actual summary quality and decide whether prompt/selection changes are needed; only then consider bumping `PROMPT_VERSION` or adding another book for compare/ask flows.

---

## Iteration 1 · 2026-03-28 22:44:49 UTC

**Progress:** Address the previously observed missing-tool environment issue first, then verify and minimally stabilize the in-progress `review-eval` CLI addition without expanding scope.

**Decisions:** approved — executor exit 0. Validation: `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" --version'` → passed; `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" -m pytest -q tests/test_review_eval.py'` → missing_tool; `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" rag_cli.py --help'` → missing_tool; `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" rag_cli.py review-eval digital-minimalism || true'` → passed.

**Assumptions:** Primary risk is spending too much effort on environment bootstrapping instead of the small CLI task. Keep environment handling minimal and non-invasive. Secondary risk is overfitting the review parser/output to one evaluation artifact shape; keep parsing best-effort and tests fixture-driven. Another risk is introducing CLI/help regressions while wiring the new command.

**Open questions:**
- Missing tool/dep for `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" -m pytest -q tests/test_review_eval.py'` — needs env fix
- Missing tool/dep for `bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif command -v python3 >/dev/null 2>&1; then PY=$(command -v python3); else PY=$(command -v python); fi; "$PY" rag_cli.py --help'` — needs env fix

**Next:** If this validates, the next iteration should actually use the stabilized `review-eval` output to inspect `digital-minimalism` summary quality and decide whether prompt/selection changes are warranted. Only after that should we consider bumping `PROMPT_VERSION` or adding another book for compare/ask flows.

---



## Project Memory (at time of snapshot)

# Project Memory

Stable facts about this project. Edit manually when fundamentals change.
The orchestrator reads this on every planning call.

## Purpose

Local book analysis tool. Two pipelines:
1. **RAG pipeline** (`rag_cli.py` + `rag/`) — primary, active development.
   Ingest → chunk → embed → select windows → summarize → evaluate → navigate → cross-synthesize.
2. **CrewAI pipeline** (`main.py` + `crews/book_summary/`) — simpler, multi-agent, may be legacy.

## Architecture

| Module | Role |
|---|---|
| `rag/ingest.py` | Load PDF/txt/md, clean text, detect structure, chunk, embed, register |
| `rag/chunker.py` | Page-level state machine → sections; overlapping chunk generation |
| `rag/analysis.py` | Windowed summarization with MMR content-diversity selection |
| `rag/cache.py` | File-backed content-addressed cache; keyed by PROMPT_VERSION + model + quality |
| `rag/retrieval.py` | ChromaDB vector search |
| `rag/evaluate.py` | Quality assessment of summaries against source chunks |
| `rag/navigation.py` | trace (idea → sections) and explore (section detail) |
| `rag/synthesis.py` | Cross-book comparison and Q&A |
| `rag_cli.py` | Main CLI entry point |
| `rag_config.yaml` | All tunable settings |

## Key invariants

- Fully local: Ollama for LLM + embeddings; ChromaDB for vectors. No external API calls.
- Data layout: `./data/cache/<book_id>/`, `./data/results/<book_id>/`, `./data/vectorstore/`
- Cache invalidation: bump `PROMPT_VERSION` in `rag/cache.py` (currently `"v4"`).
- Section types summarizable: `{introduction, chapter, conclusion, epilogue, appendix}`
- Section types skippable: `{front_matter, toc, acknowledgments, notes, index, about_author, unknown}`
- Config defaults live in `rag/config.py` dataclasses; overrides in `rag_config.yaml`.

## Constraints

- Ollama must be running (`ollama serve`) before any ingest/summarize/ask commands.
- Embedding model: `nomic-embed-text`. Generation model: `llama3.1`.
- Python 3.13, venv at `.venv/`. No package manager — plain pip.
- Currently ingested: `digital-minimalism` (test book).

## Key file paths

- `rag_config.yaml` — tuning knobs (chunking, window size, budget, MMR lambda)
- `rag/cache.py:PROMPT_VERSION` — bump when prompts change to invalidate stale cache
- `data/results/<book_id>/book_summary.md` — final output per book
- `data/results/<book_id>/evaluations/` — per-section and book-level eval reports
- `scripts/validate_rag.sh` — smoke-test commands

