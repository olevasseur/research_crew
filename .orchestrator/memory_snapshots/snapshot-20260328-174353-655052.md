# Memory Snapshot — 20260328-174353-655052

## Working Memory

# Working Memory

## Current goal
Harden the RAG pipeline; CrewAI is not the active focus.

## Latest progress
- `analysis.py` refactored for MMR-based content-diverse window selection; added `mmr_lambda` and `selection_strategy` config.
- Added `rag/evaluate.py` and `rag_cli.py evaluate`; outputs go to `data/results/<book_id>/evaluations/`.
- Added `rag/navigation.py` with `trace` and `explore`, plus `scripts/validate_rag.sh`.
- `navigation.py` tested/fixed end-to-end on `digital-minimalism`: cleaned preview/snippet formatting and chunk-id display; now readable with no known regressions.
- `digital-minimalism` remains the main test book; summary and eval artifacts exist.

## Active assumptions
- `llama3.1` via Ollama is sufficient for current summarization experiments.
- Summary quality has not been meaningfully reviewed yet beyond generating one eval.
- If prompts change, bump `PROMPT_VERSION` from `v4`.

## Open questions
- Is the `digital-minimalism` summary actually good enough? Eval exists but still needs review.
- Is CrewAI being retained at all, or effectively dead code?
- There is older unfinished work around `fiction-whois`: command execution failed end-to-end on `pale-criminal`/`Gunther`; likely needs code fix if that feature is resumed.

## What matters next
- Review the `digital-minimalism` evaluation output and decide whether summary quality is acceptable.
- Add a second book to exercise multi-book `compare` and `ask` flows.
- If eval reveals weaknesses, revise summary prompts/selection and bump `PROMPT_VERSION`.
## Iteration 2 · 2026-03-28 17:02:34 UTC

**Progress:** Fix the failing `fiction-whois` command end-to-end for the real `pale-criminal`/`Gunther` case, ensuring it reads `fiction_state.json`, respects the requested chapter cutoff, prints the requested fields, and produces validation artifacts under `/tmp` including the generated file list.

**Decisions:** approved — executor exit 0. Validation: `find . -name 'fiction_state.json' > /tmp/fiction_state_files.txt 2>&1` → passed; `(.venv/bin/python rag_cli.py --help || python3 rag_cli.py --help) > /tmp/rag_cli_help.txt 2>&1` → passed; `(.venv/bin/python rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther" || python3 rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther") > /tmp/fiction_whois_gunther.txt 2>&1` → passed; `printf '%s
' /tmp/fiction_state_files.txt /tmp/rag_cli_help.txt /tmp/fiction_whois_gunther.txt > /tmp/fiction_whois_generated_files.txt` → passed.

**Assumptions:** The main risk is that the real `fiction_state.json` schema is more irregular than expected, so a narrow fix may still miss some nested structure or chapter numbering convention and either crash or leak future information. Another risk is that the current working-tree edits in `rag/fiction.py` and `rag_cli.py` are partially complete; careless changes could regress the existing CLI wiring. Validation should therefore focus on the exact failing command and inspect the generated output file.

**Next:** If this passes, the next iteration should add a focused automated test using a tiny fixture `fiction_state.json` that locks in chapter-cutoff filtering and case-insensitive/alias-aware character lookup, preventing future regressions in `fiction-whois`.

---

## Iteration 3 · 2026-03-28 17:36:03 UTC

**Progress:** Add a small automated regression test for `fiction-whois` that locks in chapter-cutoff filtering and character lookup behavior, while preserving the now-working real CLI flow and producing validation artifacts under `/tmp` including the generated file list.

**Decisions:** approved — executor exit 0. Validation: `(.venv/bin/python -m pytest tests -k fiction_whois || python3 -m pytest tests -k fiction_whois) > /tmp/fiction_whois_pytest.txt 2>&1` → passed; `(.venv/bin/python rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther" || python3 rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther") > /tmp/fiction_whois_gunther_regression.txt 2>&1` → passed; `printf '%s\n' /tmp/fiction_whois_pytest.txt /tmp/fiction_whois_gunther_regression.txt > /tmp/fiction_whois_test_generated_files.txt` → passed.

**Assumptions:** The biggest risk is that the current `fiction_state.json` schema is irregular enough that a synthetic fixture may not match the real parsing path, producing a test that passes without protecting the real behavior. A second risk is that there may be no existing clean function boundary, forcing CLI-level tests that are a bit more brittle. Also, if alias lookup is only partially implemented, adding support could accidentally change matching precedence; keep any code change minimal and verify the real Gunther command still works.

**Next:** If these tests land and the real regression check still passes, the next step should be either a second fixture covering another schema variation from the real `fiction_state.json`, or a small review of the actual `fiction-whois` output formatting/content quality for a few characters to ensure the printed report is consistently useful.

---

## Iteration 4 · 2026-03-28 17:43:53 UTC

**Progress:** Review and lightly harden the `fiction-whois` output quality against the real `pale-criminal` data by validating the exact requested fields are present, no obvious future-information leakage appears at the chapter cutoff, and the required `/tmp` validation artifacts include a file listing of generated outputs.

**Decisions:** approved — executor exit 0. Validation: `(.venv/bin/python -m pytest tests -k fiction_whois || python3 -m pytest tests -k fiction_whois) > /tmp/fiction_whois_pytest_round2.txt 2>&1` → passed; `(.venv/bin/python rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther" || python3 rag_cli.py fiction-whois pale-criminal --chapter 10 --character "Gunther") > /tmp/fiction_whois_gunther_round2.txt 2>&1` → passed; `grep -E "role|aliases|first seen|relationships known so far|recent events involving|places associated so far" -i /tmp/fiction_whois_gunther_round2.txt > /tmp/fiction_whois_field_check.txt 2>&1 || true` → passed; `printf '%s\n' /tmp/fiction_whois_pytest_round2.txt /tmp/fiction_whois_gunther_round2.txt /tmp/fiction_whois_field_check.txt > /tmp/fiction_whois_round2_generated_files.txt` → passed.

**Assumptions:** Main risk is that the real `fiction_state.json` may encode some fields sparsely or inconsistently, so improving labels/formatting could accidentally mask missing underlying data or alter output expected by tests. Another risk is that a grep-based field check only confirms labels, not semantic correctness, so the executor should still manually inspect the command output for obvious future-information leakage and readability issues.

**Next:** If this passes with no or minimal code changes, the feature is likely complete for the requested scope. A final follow-up, only if needed, would be one additional fixture or assertion covering output labels/sections so formatting regressions are caught automatically.

---



## Project Memory (at time of snapshot)

# Project Memory

Stable facts about this project. Edit manually when fundamentals change.
The orchestrator reads this on every planning call.

## Purpose

Local book analysis tool. Two pipelines:
1. **RAG pipeline** (`rag_cli.py` + `rag/`) — primary, active development.
   Ingest → chunk → embed → select windows → summarize → evaluate → navigate → cross-synthesize.
2. **CrewAI pipeline** (`main.py` + `crews/book_summary/`) — simpler, multi-agent, may be legacy.

## Architecture

| Module | Role |
|---|---|
| `rag/ingest.py` | Load PDF/txt/md, clean text, detect structure, chunk, embed, register |
| `rag/chunker.py` | Page-level state machine → sections; overlapping chunk generation |
| `rag/analysis.py` | Windowed summarization with MMR content-diversity selection |
| `rag/cache.py` | File-backed content-addressed cache; keyed by PROMPT_VERSION + model + quality |
| `rag/retrieval.py` | ChromaDB vector search |
| `rag/evaluate.py` | Quality assessment of summaries against source chunks |
| `rag/navigation.py` | trace (idea → sections) and explore (section detail) |
| `rag/synthesis.py` | Cross-book comparison and Q&A |
| `rag_cli.py` | Main CLI entry point |
| `rag_config.yaml` | All tunable settings |

## Key invariants

- Fully local: Ollama for LLM + embeddings; ChromaDB for vectors. No external API calls.
- Data layout: `./data/cache/<book_id>/`, `./data/results/<book_id>/`, `./data/vectorstore/`
- Cache invalidation: bump `PROMPT_VERSION` in `rag/cache.py` (currently `"v4"`).
- Section types summarizable: `{introduction, chapter, conclusion, epilogue, appendix}`
- Section types skippable: `{front_matter, toc, acknowledgments, notes, index, about_author, unknown}`
- Config defaults live in `rag/config.py` dataclasses; overrides in `rag_config.yaml`.

## Constraints

- Ollama must be running (`ollama serve`) before any ingest/summarize/ask commands.
- Embedding model: `nomic-embed-text`. Generation model: `llama3.1`.
- Python 3.13, venv at `.venv/`. No package manager — plain pip.
- Currently ingested: `digital-minimalism` (test book).

## Key file paths

- `rag_config.yaml` — tuning knobs (chunking, window size, budget, MMR lambda)
- `rag/cache.py:PROMPT_VERSION` — bump when prompts change to invalidate stale cache
- `data/results/<book_id>/book_summary.md` — final output per book
- `data/results/<book_id>/evaluations/` — per-section and book-level eval reports
- `scripts/validate_rag.sh` — smoke-test commands

