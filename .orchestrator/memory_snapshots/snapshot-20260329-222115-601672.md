# Memory Snapshot — 20260329-222115-601672

## Working Memory

# Working Memory

## Current goal
Harden the RAG pipeline; CrewAI is not the active focus.

## Latest progress
- RAG hardening landed: MMR selection in `rag/analysis.py`, eval flow in `rag/evaluate.py`, and navigation in `rag/navigation.py` (`trace`/`explore`).
- `navigation.py` works end-to-end on `digital-minimalism`; output formatting is readable.
- `review-eval` CLI flow is working; `python rag_cli.py review-eval digital-minimalism` passed in fallback-env validation.
- `fiction-whois` real CLI flow passes on `pale-criminal`/`Gunther`, reads `fiction_state.json`, respects chapter cutoff, and has regression coverage.
- Repo hygiene/UI side work completed recently: TODO inventory expanded to TODO/FIXME/HACK/NOTE with count-checked test; fiction UI now hides for non-fiction and has small usability/error-rendering improvements.

## Open questions
- Is the `digital-minimalism` summary actually good enough after reviewing eval output?
- Is CrewAI retained at all, or effectively dead code?
- For `fiction-whois`, add one more fixture/assertion for schema variation or output-label regression protection?
- This runner still lacks pieces for some validation paths: full `python -m pytest` and `rag_cli.py --help` can fail due to missing tools/deps.

## Active assumptions
- `llama3.1` via Ollama is sufficient for current summarization experiments.
- Summary quality has not yet been meaningfully reviewed beyond generating eval/review output.
- If prompts change, bump `PROMPT_VERSION` from `v4`.
- `fiction_state.json` may still vary enough that current tests miss edge cases.

## What matters next
- Use `review-eval` to inspect `digital-minimalism` quality and decide whether prompt/selection changes are needed.
- If eval is weak, revise summary prompts/selection and bump `PROMPT_VERSION`.
- Add a second book to exercise multi-book `compare` and `ask` flows.
- Optionally add one more `fiction-whois` fixture/assertion for schema variation/formatting.## Iteration 0 · 2026-03-29 21:44:09 UTC

**Progress:** Make the fiction panel labels clearer by renaming three chapter field labels in `rag_ui.html` only, while preserving all existing behavior and producing short validation output written to `/tmp/fiction_ui_label_validation.txt`.

**Decisions:** approved — executor exit 0. Validation: `python - <<'PY' > /tmp/fiction_ui_label_validation.txt
from pathlib import Path
text = Path('rag_ui.html').read_text()
labels = ['Current chapter', 'Compare from chapter', 'Compare to chapter']
print('Label check:')
for label in labels:
    print(f'- {label}:', 'OK' if label in text else 'MISSING')
PY` → passed; `sh -c 'python -m http.server 8000 >/tmp/fiction_ui_http.log 2>&1 & SERVER_PID=$!; sleep 1; STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/rag_ui.html); { echo "Page load:"; echo "- http://127.0.0.1:8000/rag_ui.html -> ${STATUS}"; } >> /tmp/fiction_ui_label_validation.txt; kill $SERVER_PID >/dev/null 2>&1; wait $SERVER_PID 2>/dev/null || true; test "$STATUS" = "200"'` → implementation_failure.

**Assumptions:** `rag_ui.html` is already modified in the working tree, so keep the edit tightly scoped to the three fiction label strings only and avoid incidental formatting or unrelated UI changes. If the page references assets that do not affect the top-level HTML response, the required validation is still specifically the `rag_ui.html` HTTP 200 check.

**Open questions:**
- `sh -c 'python -m http.server 8000 >/tmp/fiction_ui_http.log 2>&1 & SERVER_PID=$!; sleep 1; STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/rag_ui.html); { echo "Page load:"; echo "- http://127.0.0.1:8000/rag_ui.html -> ${STATUS}"; } >> /tmp/fiction_ui_label_validation.txt; kill $SERVER_PID >/dev/null 2>&1; wait $SERVER_PID 2>/dev/null || true; test "$STATUS" = "200"'` failed — code fix needed

**Next:** After this lands, the next safe step would be another tiny fiction-panel usability tweak only if requested, such as clarifying placeholder text or helper text without changing behavior. For now, keep this iteration limited to label text plus validation output.

---

## Iteration 0 · 2026-03-29 22:17:40 UTC

**Progress:** Add a single harmless HTML comment to rag_ui.html and intentionally leave validation in a failed state to test orchestrator UI failure handling, while writing validation output to /tmp/orchestrator_validation_failure_test.txt.

**Decisions:** approved — executor exit 0. Validation: `grep -n "orchestrator validation failure test" rag_ui.html >> /tmp/orchestrator_validation_failure_test.txt` → passed; `python -c "import sys; sys.exit(1)" >> /tmp/orchestrator_validation_failure_test.txt 2>&1` → implementation_failure.

**Assumptions:** Low risk. The only intended file edit is a single HTML comment in rag_ui.html. Main caution is to avoid touching unrelated existing working tree changes and to ensure the intentionally failing validation still writes output to the target file while causing overall validation failure.

**Open questions:**
- `python -c "import sys; sys.exit(1)" >> /tmp/orchestrator_validation_failure_test.txt 2>&1` failed — code fix needed

**Next:** After this, inspect whether the orchestrator UI correctly marks the iteration failed, avoids showing succeeded, and displays the warning/callout above executor output on validation failure. No follow-up code change is needed unless the UI behavior itself is incorrect.

---

## Iteration 0 · 2026-03-29 22:21:15 UTC

**Progress:** Create a tiny, harmless UI-only change in rag_ui.html and preserve the intentionally failing validation setup so the orchestrator UI can be tested for failed-validation handling.

**Decisions:** approved — executor exit 0. Validation: `grep -n "orchestrator validation failure test" rag_ui.html` → passed; `python -c "import sys; sys.exit(1)"` → implementation_failure.

**Assumptions:** Very low risk if the edit is limited to a single HTML comment. Main risk is accidentally modifying unrelated parts of rag_ui.html or other files in a dirty working tree. The second validation command is intentionally failing and should remain so.

**Open questions:**
- `python -c "import sys; sys.exit(1)"` failed — code fix needed

**Next:** After this iteration, verify in the orchestrator UI that the run is marked failed, not succeeded, and that the validation-failure warning/callout appears above executor output. No further code change should be needed unless the UI behavior is wrong.

---



## Project Memory (at time of snapshot)

# Project Memory

Stable facts about this project. Edit manually when fundamentals change.
The orchestrator reads this on every planning call.

## Purpose

Local book analysis tool. Two pipelines:
1. **RAG pipeline** (`rag_cli.py` + `rag/`) — primary, active development.
   Ingest → chunk → embed → select windows → summarize → evaluate → navigate → cross-synthesize.
2. **CrewAI pipeline** (`main.py` + `crews/book_summary/`) — simpler, multi-agent, may be legacy.

## Architecture

| Module | Role |
|---|---|
| `rag/ingest.py` | Load PDF/txt/md, clean text, detect structure, chunk, embed, register |
| `rag/chunker.py` | Page-level state machine → sections; overlapping chunk generation |
| `rag/analysis.py` | Windowed summarization with MMR content-diversity selection |
| `rag/cache.py` | File-backed content-addressed cache; keyed by PROMPT_VERSION + model + quality |
| `rag/retrieval.py` | ChromaDB vector search |
| `rag/evaluate.py` | Quality assessment of summaries against source chunks |
| `rag/navigation.py` | trace (idea → sections) and explore (section detail) |
| `rag/synthesis.py` | Cross-book comparison and Q&A |
| `rag_cli.py` | Main CLI entry point |
| `rag_config.yaml` | All tunable settings |

## Key invariants

- Fully local: Ollama for LLM + embeddings; ChromaDB for vectors. No external API calls.
- Data layout: `./data/cache/<book_id>/`, `./data/results/<book_id>/`, `./data/vectorstore/`
- Cache invalidation: bump `PROMPT_VERSION` in `rag/cache.py` (currently `"v4"`).
- Section types summarizable: `{introduction, chapter, conclusion, epilogue, appendix}`
- Section types skippable: `{front_matter, toc, acknowledgments, notes, index, about_author, unknown}`
- Config defaults live in `rag/config.py` dataclasses; overrides in `rag_config.yaml`.

## Constraints

- Ollama must be running (`ollama serve`) before any ingest/summarize/ask commands.
- Embedding model: `nomic-embed-text`. Generation model: `llama3.1`.
- Python 3.13, venv at `.venv/`. No package manager — plain pip.
- Currently ingested: `digital-minimalism` (test book).

## Key file paths

- `rag_config.yaml` — tuning knobs (chunking, window size, budget, MMR lambda)
- `rag/cache.py:PROMPT_VERSION` — bump when prompts change to invalidate stale cache
- `data/results/<book_id>/book_summary.md` — final output per book
- `data/results/<book_id>/evaluations/` — per-section and book-level eval reports
- `scripts/validate_rag.sh` — smoke-test commands

